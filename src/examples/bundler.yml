description: >
  Using cocoapods in bundler environment.

usage:
  version: 2.1
  orbs:
    pod: xxkrashxx/pod@1.1.0
  workflows:
    basic-usage:
      jobs:
        - checkout
        - pod/install:
            with-bundler: true
